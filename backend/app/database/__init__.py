"""
Database package.

Contains the SQLAlchemy declarative base (`base.py`), the async engine and
session factory (`session.py`), and a small startup/shutdown helper
(`init_db.py`) used by the application lifespan.
"""
