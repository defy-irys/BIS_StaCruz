"""
Utils package.

Small, generic, dependency-free helper functions that don't belong to any
particular layer (core/database/auth/etc.). Keep this package free of
business logic - it should only ever contain pure, reusable utilities.
"""
